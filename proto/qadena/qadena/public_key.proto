syntax = "proto3";
package qadena.qadena;

option go_package = "qadena_v3/x/qadena/types";

import "qadena/qadena/share.proto";

message PublicKey {
  string pubKID = 1; 
  string pubKType = 2;  
  string pubK = 3;
  repeated Share shares = 4;
  bytes remoteReport = 5;
}

